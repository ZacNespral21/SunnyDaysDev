import { isMobile } from 'react-device-detect';
module.exports = global.config = {
  assets: {
    logo: {
      main: "./assets/logo-no-background.png",
      icon: './assets/logo-no-background.png',
    }
  }
  // other global config variables you wish
};